<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BBS</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        .panel-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 20px;
        }

        .panel {
            flex: 1 0 calc(25% - 20px);
            border: 1px solid #ccc;
            padding: 10px;
            margin-bottom: 20px;
        }

        .like-button {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 5px 10px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 14px;
            cursor: pointer;
            margin-top: 10px;
        }

        .post-text {
            width: 100%;
            box-sizing: border-box;
            margin-top: 10px;
        }

        .divider {
            border-bottom: 1px solid #ccc;
            margin-bottom: 10px;
        }

        .time-slot {
            border: 1px solid #ccc;
            padding: 10px;
            margin-bottom: 20px;
        }

        .total-time {
            margin-top: 10px;
        }
    </style>
</head>
<body>

<% total_time = 0 %>  

  <% @history.each_slice(3) do |slice| %>
    <div class="panel-container">
      <div class="divider"></div>
      <% slice.each_with_index do |t, index| %>
        <% data = t.history.split(',') %>
        <div class="time-slot">
          <h2><%= data.first %> - <%= data.last %></h2>
          <div class="divider"></div>

          <% (0..10).each do |panel_index| %>
            <% panel_data_index = panel_index * 3 %>
            <% break if panel_data_index >= data.size %>

            <!-- 各パネルの表示 -->
            <div class="panel">
              <h3><%= data[panel_data_index] %></h3>
              <p>移動手段: <%= data[panel_data_index + 1] %></p>
              <p>移動時間: <%= data[panel_data_index + 2] %></p>
              <p>目的地: <%= data[panel_data_index + 3] %></p>
            </div>
          <% end %>

          <!-- 合計時間の表示 -->
          <div class="total-time">
            <h3>合計時間: <%= total_time %> 分</h3>
          </div>

          <!-- 必要に応じて他の情報を表示 -->

        </div>
        <% total_time += data[2].to_i %>
      <% end %>
    </div>
    
  <% end %>

    <%= link_to 'Course検索へ', '/courses/new' %>
    <br>
    <%= link_to 'サイトトップへ', '/courses/siteTop' %>

</body>


</html>
